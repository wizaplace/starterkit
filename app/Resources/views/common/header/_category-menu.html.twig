{% set categories = categoryTree() %}

<div class="title hidden-md hidden-lg">Catégories</div>

<ul class="category-menu">
    {% for category in categories %}

        <li class="category">

            <div class="category-name-group">
                {% if category.children %}
                    <i class="menu-toggle fa fa-plus hidden-md hidden-lg"></i>
                {% endif %}

                <a href="{{ path('category', {'slug': category.category.slug}) }}" class="category-name">{{ category.category.name }}</a>
            </div>

            <div class="wrapper">

                {% if category.children %}
                    <ul class="category-sub-menu">

                        {% for category in category.children %}

                            <li class="category">
                                <a href="{{ path('category', {'slug': category.category.slug}) }}" class="category-name">{{ category.category.name }}</a>
                                <ul class="category-sub-menu">

                                    {% for category in category.children %}

                                        <li class="category">
                                            <a href="{{ path('category', {'slug': category.category.slug}) }}" class="category-name">{{ category.category.name }}</a>
                                        </li>

                                    {% endfor %}
                                </ul>
                            </li>
                        {% endfor %}
                    </ul>
                {% endif %}
            </div>
        </li>
    {% endfor %}
</ul>
